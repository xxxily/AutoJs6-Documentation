import{_ as o,o as t,c as i,a2 as a}from"./chunks/framework.BZO4qERu.js";const h=JSON.parse('{"title":"自动化 (Automator)","description":"","frontmatter":{},"headers":[],"relativePath":"automator.md","filePath":"automator.md","lastUpdated":1780845803000}'),d={name:"automator.md"};function c(s,e,n,p,l,r){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="自动化-automator" tabindex="-1">自动化 (Automator)</h1><h2 id="_6-7-0-源码校对" tabindex="-1">6.7.0 源码校对</h2><p>本页已按 AutoJs6 <code>6.7.0</code> (<code>ed3eb10e88db5a8425fd94bdddefa4176e5e1c94</code>) 对照以下源码路径校对:</p><ul><li><code>app/src/main/java/org/autojs/autojs/runtime/ScriptRuntime.kt</code></li><li><code>app/src/main/java/org/autojs/autojs/runtime/api/augment/automator/Auto.kt</code></li><li><code>app/src/main/java/org/autojs/autojs/runtime/api/augment/automator/Automator.kt</code></li><li><code>app/src/main/java/org/autojs/autojs/runtime/api/augment/automator/RootAutomator.kt</code></li><li><code>app/src/main/java/org/autojs/autojs/runtime/api/augment/automator/RootAutomatorNativeObject.kt</code></li><li><code>app/src/main/java/org/autojs/autojs/core/accessibility/SimpleActionAutomator.kt</code></li><li><code>app/src/main/java/org/autojs/autojs/core/inputevent/RootAutomator.java</code></li><li><code>app/src/main/java/org/autojs/autojs/engine/RootAutomatorEngine.kt</code></li></ul><p>运行时中 <code>auto</code>, <code>automator</code>, <code>selector</code>, <code>RootAutomator</code> 均由 <code>ScriptRuntime.augment(...)</code> 注入. <code>RootAutomator</code> 仅注入构造器, 不存在 <code>$RootAutomator</code> 模块对象.</p><p>当前源码公开的 <code>auto</code> / <code>$auto</code> 入口包括 <code>start</code>, <code>enable</code>, <code>stop</code>, <code>disable</code>, <code>hasInstance</code>, <code>hasService</code>, <code>exists</code>, <code>isRunning</code>, <code>isOperational</code>, <code>stateListener</code>, <code>registerEvent</code>, <code>registerEvents</code>, <code>removeEvent</code>, <code>removeEvents</code>, <code>waitFor</code>, <code>setMode</code>, <code>setFlags</code>, <code>setWindowFilter</code>, <code>launchSettings</code>, <code>clearCache</code>, <code>currentPackage</code>, <code>currentActivity</code>, <code>currentComponent</code>; getter 包括 <code>service</code>, <code>services</code>, <code>windows</code>, <code>root</code>, <code>rootInActiveWindow</code>, <code>windowRoots</code>, <code>state</code>.</p><p>当前源码公开的 <code>automator</code> / <code>$automator</code> 入口包括全局坐标和系统动作: <code>click</code>, <code>longClick</code>, <code>press</code>, <code>swipe</code>, <code>gesture</code>, <code>gestureAsync</code>, <code>gestures</code>, <code>gesturesAsync</code>, <code>scrollDown</code>, <code>scrollUp</code>, <code>input</code>, <code>setText</code>, <code>back</code>, <code>home</code>, <code>powerDialog</code>, <code>notifications</code>, <code>quickSettings</code>, <code>recents</code>, <code>splitScreen</code>; 模块补充入口包括 <code>isServiceRunning</code>, <code>ensureService</code>, <code>waitForService</code>, <code>captureScreen</code>, <code>lockScreen</code>, <code>takeScreenshot</code>, <code>headsethook</code>, <code>accessibilityButton</code>, <code>accessibilityButtonChooser</code>, <code>accessibilityShortcut</code>, <code>accessibilityAllApps</code>, <code>dismissNotificationShade</code>.</p><p>坐标触摸动作在 <code>6.7.0</code> 源码中走无障碍 <code>dispatchGesture</code>, 因此依赖可用的无障碍服务; <code>captureScreen()</code> 要求 Android R / API 30+ 的无障碍截图能力.</p><p><code>RootAutomator</code> 构造要求 root 或可用 Shizuku. 底层公开 <code>sendEvent</code>, <code>touch</code>, <code>setScreenMetrics</code>, <code>touchX</code>, <code>touchY</code>, <code>sendSync</code>, <code>sendMtSync</code>, <code>tap</code>, <code>swipe</code>, <code>press</code>, <code>longPress</code>, <code>touchDown</code>, <code>touchUp</code>, <code>touchMove</code>, <code>getDefaultId</code>, <code>setDefaultId</code>, <code>exit</code>. 默认触点 ID 为 <code>0</code>.</p><h2 id="简易自动化-simpleactionautomator" tabindex="-1">简易自动化 (SimpleActionAutomator)</h2><p>待补充...</p><h2 id="高权限自动化-rootautomator" tabindex="-1">高权限自动化 (RootAutomator)</h2><p>待补充...</p><h2 id="自动化配置-automatorconfiguration" tabindex="-1">自动化配置 (AutomatorConfiguration)</h2><p>待补充...</p><h2 id="选择器-uiselector" tabindex="-1">选择器 (UiSelector)</h2><p>UiSelector (选择器), 亦可看作是 <a href="./uiObjectType">控件节点</a> 的条件筛选器, 用于通过附加不同的条件, 筛选出一个或一组活动窗口中的 <code>控件节点</code>, 并做进一步处理, 如 [ 执行 <a href="./uiObjectActionsType">控件行为</a> (点击, 长按, 设置文本等) / 判断位置 / 获取文本内容 / 获取控件特定状态 / 在 <a href="./glossaries#控件层级">控件层级</a> 中进行 <a href="./uiObjectType#m-compass">罗盘</a> 导航 ] 等.</p><p>详情参阅 <a href="./uiSelectorType">选择器 (UiSelector)</a> 章节.</p><h2 id="控件节点-uiobject" tabindex="-1">控件节点 (UiObject)</h2><p>UiObject 通常被称为 [ 控件 / 节点 / 控件节点 ], 可看做是一个通过安卓无障碍服务包装的 <a href="https://developer.android.com/reference/android/view/accessibility/AccessibilityNodeInfo" target="_blank" rel="noreferrer">AccessibilityNodeInfo</a> 对象, 代表一个当前活动窗口中的节点, 通过此节点可收集控件信息或执行控件行为, 进而实现一系列自动化操作.</p><p>详情参阅 <a href="./uiObjectType">控件节点 (UiObject)</a> 章节.</p><h2 id="控件集合-uiobjectcollection" tabindex="-1">控件集合 (UiObjectCollection)</h2><p>UiObjectCollection 代表 <a href="./uiObjectType">控件节点 (UiObject)</a> 的对象集合.</p><p>详情参阅 <a href="./uiObjectCollectionType">控件集合 (UiObjectCollection)</a> 章节.</p><h2 id="控件节点行为-uiobjectactions" tabindex="-1">控件节点行为 (UiObjectActions)</h2><p>UiObjectActions 是一个 Java 接口, 代表 <a href="./uiObjectType">控件节点 (UiObject)</a> 的行为集合.</p><p>详情参阅 <a href="./uiObjectActionsType">控件节点行为 (UiObjectActions)</a> 章节.</p><hr><h1 id="基于坐标的触摸模拟" tabindex="-1">基于坐标的触摸模拟</h1><p>本章节介绍使用坐标进行点击、滑动的函数. 在 AutoJs6 <code>6.7.0</code> 中, 这些全局坐标动作主要通过无障碍服务的手势分发实现, 因此需要确保无障碍服务可用.</p><p>要获取要点击的位置的坐标, 可以在开发者选项中开启&quot;指针位置&quot;.</p><p>基于坐标的脚本通常会有分辨率的问题, 这时可以通过<code>setScreenMetrics()</code>函数来进行自动坐标放缩. 这个函数会影响本章节的所有点击、长按、滑动等函数. 通过设定脚本设计时的分辨率, 使得脚本在其他分辨率下自动放缩坐标.</p><p>控件和坐标也可以相互结合. 一些控件是无法点击的(clickable为false), 无法通过<code>.click()</code>函数来点击, 这时可通过以下方式点击控件中心坐标：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//获取这个控件</span></span>
<span class="line"><span>var widget = id(&quot;xxx&quot;).findOne();</span></span>
<span class="line"><span>//获取其中心位置并点击</span></span>
<span class="line"><span>click(widget.bounds().centerX(), widget.bounds().centerY());</span></span>
<span class="line"><span>//如果用root权限则用Tap</span></span></code></pre></div><h2 id="setscreenmetrics-width-height" tabindex="-1">setScreenMetrics(width, height)</h2><ul><li>width {number} 屏幕宽度, 单位像素</li><li>height {number} 屏幕高度, 单位像素</li></ul><p>设置脚本坐标点击所适合的屏幕宽高. 如果脚本运行时, 屏幕宽度不一致会自动放缩坐标.</p><p>例如在1920*1080的设备中, 某个操作的代码为</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>setScreenMetrics(1080, 1920);</span></span>
<span class="line"><span>click(800, 200);</span></span>
<span class="line"><span>longClick(300, 500);</span></span></code></pre></div><p>那么在其他设备上AutoJs会自动放缩坐标以便脚本仍然有效. 例如在540 * 960的屏幕中<code>click(800, 200)</code>实际上会点击位置(400, 100).</p><h1 id="安卓7-0以上的触摸和手势模拟" tabindex="-1">安卓7.0以上的触摸和手势模拟</h1><p><strong>注意以下命令只有Android7.0及以上才有效</strong></p><h2 id="click-x-y" tabindex="-1">click(x, y)</h2><ul><li><code>x</code> {number} 要点击的坐标的x值</li><li><code>y</code> {number} 要点击的坐标的y值</li></ul><p>模拟点击坐标(x, y), 并返回是否点击成功. 只有在点击执行完成后脚本才继续执行.</p><p>一般而言, 只有点击过程(大约150毫秒)中被其他事件中断(例如用户自行点击)才会点击失败.</p><p>使用该函数模拟连续点击时可能有点击速度过慢的问题, 这时可以用<code>press()</code>函数代替.</p><h2 id="longclick-x-y" tabindex="-1">longClick(x, y)</h2><ul><li><code>x</code> {number} 要长按的坐标的x值</li><li><code>y</code> {number} 要长按的坐标的y值</li></ul><p>模拟长按坐标(x, y), 并返回是否成功. 只有在长按执行完成（大约600毫秒）时脚本才会继续执行.</p><p>一般而言, 只有长按过程中被其他事件中断(例如用户自行点击)才会长按失败.</p><h2 id="press-x-y-duration" tabindex="-1">press(x, y, duration)</h2><ul><li><code>x</code> {number} 要按住的坐标的x值</li><li><code>y</code> {number} 要按住的坐标的y值</li><li><code>duration</code> {number} 按住时长, 单位毫秒</li></ul><p>模拟按住坐标(x, y), 并返回是否成功. 只有按住操作执行完成时脚本才会继续执行.</p><p>如果按住时间过短, 那么会被系统认为是点击；如果时长超过500毫秒, 则认为是长按.</p><p>一般而言, 只有按住过程中被其他事件中断才会操作失败.</p><p>一个连点器的例子如下：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//循环100次</span></span>
<span class="line"><span>for(var i = 0; i &lt; 100; i++){</span></span>
<span class="line"><span>  //点击位置(500, 1000), 每次用时1毫秒</span></span>
<span class="line"><span>  press(500, 1000, 1);</span></span>
<span class="line"><span>}</span></span></code></pre></div><h2 id="swipe-x1-y1-x2-y2-duration" tabindex="-1">swipe(x1, y1, x2, y2, duration)</h2><ul><li><code>x1</code> {number} 滑动的起始坐标的x值</li><li><code>y1</code> {number} 滑动的起始坐标的y值</li><li><code>x2</code> {number} 滑动的结束坐标的x值</li><li><code>y2</code> {number} 滑动的结束坐标的y值</li><li><code>duration</code> {number} 滑动时长, 单位毫秒</li></ul><p>模拟从坐标(x1, y1)滑动到坐标(x2, y2), 并返回是否成功. 只有滑动操作执行完成时脚本才会继续执行.</p><p>一般而言, 只有滑动过程中被其他事件中断才会滑动失败.</p><h2 id="gesture-duration-x1-y1-x2-y2" tabindex="-1">gesture(duration, [x1, y1], [x2, y2], ...)</h2><ul><li><code>duration</code> {number} 手势的时长</li><li>[x, y] ... 手势滑动路径的一系列坐标</li></ul><p>模拟手势操作. 例如<code>gesture(1000, [0, 0], [500, 500], [500, 1000])</code>为模拟一个从(0, 0)到(500, 500)到(500, 100)的手势操作, 时长为2秒.</p><h2 id="gestures-delay1-duration1-x1-y1-x2-y2-delay2-duration2-x3-y3-x4-y4" tabindex="-1">gestures([delay1, duration1, [x1, y1], [x2, y2], ...], [delay2, duration2, [x3, y3], [x4, y4], ...], ...)</h2><p>同时模拟多个手势. 每个手势的参数为[delay, duration, 坐标], delay为延迟多久(毫秒)才执行该手势；duration为手势执行时长；坐标为手势经过的点的坐标. 其中delay参数可以省略, 默认为0.</p><p>例如手指捏合：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gestures([0, 500, [800, 300], [500, 1000]],</span></span>
<span class="line"><span>         [0, 500, [300, 1500], [500, 1000]]);</span></span></code></pre></div><h1 id="rootautomator" tabindex="-1">RootAutomator</h1><p>RootAutomator 是一个使用 root 或 Shizuku 后端模拟触摸的对象, 用它可以完成触摸与多点触摸, 并且这些动作的执行没有延迟.</p><p>一个脚本中最好只存在一个RootAutomator, 并且保证脚本结束退出他. 可以在exit事件中退出RootAutomator, 例如：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>var ra = new RootAutomator();</span></span>
<span class="line"><span>events.on(&#39;exit&#39;, function(){</span></span>
<span class="line"><span>  ra.exit();</span></span>
<span class="line"><span>});</span></span>
<span class="line"><span>//执行一些点击操作</span></span>
<span class="line"><span>...</span></span></code></pre></div><p><strong>注意以下命令需要 root 权限或可用的 Shizuku 输入设备访问能力.</strong></p><h2 id="rootautomator-tap-x-y-id" tabindex="-1">RootAutomator.tap(x, y[, id])</h2><ul><li><code>x</code> {number} 横坐标</li><li><code>y</code> {number} 纵坐标</li><li><code>id</code> {number} 多点触摸id, 可选, 默认为0, 可以通过setDefaultId指定.</li></ul><p>点击位置(x, y). 其中id是一个整数值, 用于区分多点触摸, 不同的id表示不同的&quot;手指&quot;, 例如：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>var ra = new RootAutomator();</span></span>
<span class="line"><span>//让&quot;手指1&quot;点击位置(100, 100)</span></span>
<span class="line"><span>ra.tap(100, 100, 1);</span></span>
<span class="line"><span>//让&quot;手指2&quot;点击位置(200, 200);</span></span>
<span class="line"><span>ra.tap(200, 200, 2);</span></span>
<span class="line"><span>ra.exit();</span></span></code></pre></div><p>如果不需要多点触摸, 则不需要id这个参数. 多点触摸通常用于手势或游戏操作, 例如模拟双指捏合、双指上滑等.</p><p>某些情况下可能存在tap点击无反应的情况, 这时可以用<code>RootAutomator.press()</code>函数代替.</p><h2 id="rootautomator-swipe-x1-y1-x2-y2-duration-id" tabindex="-1">RootAutomator.swipe(x1, y1, x2, y2[, duration, id])</h2><ul><li><code>x1</code> {number} 滑动起点横坐标</li><li><code>y1</code> {number} 滑动起点纵坐标</li><li><code>x2</code> {number} 滑动终点横坐标</li><li><code>y2</code> {number} 滑动终点纵坐标</li><li><code>duration</code> {number} 滑动时长, 单位毫秒, 默认值为300</li><li><code>id</code> {number} 多点触摸id, 可选, 默认为0</li></ul><p>模拟一次从(x1, y1)到(x2, y2)的时间为duration毫秒的滑动.</p><h2 id="rootautomator-press-x-y-duration-id" tabindex="-1">RootAutomator.press(x, y, duration[, id])</h2><ul><li><code>x</code> {number} 横坐标</li><li><code>y</code> {number} 纵坐标</li><li><code>duration</code> {number} 按下时长</li><li><code>id</code> {number} 多点触摸id, 可选, 默认为0</li></ul><p>模拟按下位置(x, y), 时长为duration毫秒.</p><h2 id="rootautomator-longpress-x-y-id" tabindex="-1">RootAutomator.longPress(x, y[, id])</h2><ul><li><code>x</code> {number} 横坐标</li><li><code>y</code> {number} 纵坐标</li><li><code>id</code> {number} 多点触摸id, 可选, 默认为0</li></ul><p>模拟长按位置(x, y).</p><p>以上为简单模拟触摸操作的函数. 如果要模拟一些复杂的手势, 需要更底层的函数.</p><h2 id="rootautomator-touchdown-x-y-id" tabindex="-1">RootAutomator.touchDown(x, y[, id])</h2><ul><li><code>x</code> {number} 横坐标</li><li><code>y</code> {number} 纵坐标</li><li><code>id</code> {number} 多点触摸id, 可选, 默认为0</li></ul><p>模拟手指按下位置(x, y).</p><h2 id="rootautomator-touchmove-x-y-id" tabindex="-1">RootAutomator.touchMove(x, y[, id])</h2><ul><li><code>x</code> {number} 横坐标</li><li><code>y</code> {number} 纵坐标</li><li><code>id</code> {number} 多点触摸id, 可选, 默认为0</li></ul><p>模拟移动手指到位置(x, y).</p><h2 id="rootautomator-touchup-id" tabindex="-1">RootAutomator.touchUp([id])</h2><ul><li><code>id</code> {number} 多点触摸id, 可选, 默认为0</li></ul><p>模拟手指弹起.</p><h1 id="使用root权限点击和滑动的简单命令" tabindex="-1">使用root权限点击和滑动的简单命令</h1><p>注意：本章节的函数在后续版本很可能有改动！请勿过分依赖本章节函数的副作用. 推荐使用<code>RootAutomator</code>代替本章节的触摸函数.</p><p>以下函数均需要root权限, 可以实现任意位置的点击、滑动等.</p><ul><li>这些函数通常首字母大写以表示其特殊的权限.</li><li>这些函数均不返回任何值.</li><li>并且, 这些函数的执行是异步的、非阻塞的, 在不同机型上所用的时间不同. 脚本不会等待动作执行完成才继续执行. 因此最好在每个函数之后加上适当的sleep来达到期望的效果.</li></ul><p>例如:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Tap(100, 100);</span></span>
<span class="line"><span>sleep(500);</span></span></code></pre></div><p>注意, 动作的执行可能无法被停止, 例如：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for(var i = 0; i &lt; 100; i++){</span></span>
<span class="line"><span>  Tap(100, 100);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>这段代码执行后可能会出现在任务管理中停止脚本后点击仍然继续的情况. 因此, 强烈建议在每个动作后加上延时：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for(var i = 0; i &lt; 100; i++){</span></span>
<span class="line"><span>  Tap(100, 100);</span></span>
<span class="line"><span>  sleep(500);</span></span>
<span class="line"><span>}</span></span></code></pre></div><h2 id="tap-x-y" tabindex="-1">Tap(x, y)</h2><ul><li>x, y {number} 要点击的坐标.</li></ul><p>点击位置(x, y), 您可以通过&quot;开发者选项&quot;开启指针位置来确定点击坐标.</p><h2 id="swipe-x1-y1-x2-y2-duration-1" tabindex="-1">Swipe(x1, y1, x2, y2, [duration])</h2><ul><li>x1, y1 {number} 滑动起点的坐标</li><li>x2, y2 {number} 滑动终点的坐标</li><li>duration {number} 滑动动作所用的时间</li></ul><p>滑动. 从(x1, y1)位置滑动到(x2, y2)位置.</p><h1 id="基于控件的操作" tabindex="-1">基于控件的操作</h1><p>基于控件的操作指的是选择屏幕上的控件, 获取其信息或对其进行操作. 对于一般软件而言, 基于控件的操作对不同机型有很好的兼容性；但是对于游戏而言, 由于游戏界面并不是由控件构成, 无法采用本章节的方法, 也无法使用本章节的函数. 有关游戏脚本的编写, 请参考《基于坐标的操作》.</p><p>基于控件的操作依赖于无障碍服务, 因此最好在脚本开头使用<code>auto()</code>函数来确保无障碍服务已经启用. 如果运行到某个需要权限的语句无障碍服务并没启动, 则会抛出异常并跳转到无障碍服务界面. 这样的用户体验并不好, 因为需要重新运行脚本, 后续会加入等待无障碍服务启动并让脚本继续运行的函数.</p><p>您也可以在脚本开头使用<code>&quot;auto&quot;;</code>表示这个脚本需要无障碍服务, 但是不推荐这种做法, 因为这个标记必须在脚本的最开头(前面不能有注释或其他语句、空格等), 我们推荐使用<code>auto()</code>函数来确保无障碍服务已启用.</p><h2 id="auto-mode" tabindex="-1">auto([mode])</h2><ul><li><code>mode</code> {string} 模式</li></ul><p>检查无障碍服务是否已经启用, 如果没有启用则抛出异常并跳转到无障碍服务启用界面；同时设置无障碍模式为mode. mode的可选值为：</p><ul><li><code>fast</code> 快速模式. 该模式下会启用控件缓存, 从而选择器获取屏幕控件更快. 对于需要快速的控件操作的脚本可以使用该模式, 一般脚本则没有必要使用该函数.</li><li><code>normal</code> 正常模式, 默认.</li></ul><p>如果不加mode参数, 则为正常模式.</p><p>建议使用<code>auto.waitFor()</code>和<code>auto.setMode()</code>代替该函数, 因为<code>auto()</code>函数如果无障碍服务未启动会停止脚本；而<code>auto.waitFor()</code>则会在在无障碍服务启动后继续运行.</p><p>示例：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>auto(&quot;fast&quot;);</span></span></code></pre></div><p>示例2：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>auto();</span></span></code></pre></div><h2 id="auto-waitfor" tabindex="-1">auto.waitFor()</h2><p>检查无障碍服务是否已经启用, 如果没有启用则跳转到无障碍服务启用界面, 并等待无障碍服务启动；当无障碍服务启动后脚本会继续运行.</p><p>因为该函数是阻塞的, 因此除非是有协程特性, 否则不能在ui模式下运行该函数, 建议在ui模式下使用<code>auto()</code>函数.</p><h2 id="auto-setmode-mode" tabindex="-1">auto.setMode(mode)</h2><ul><li><code>mode</code> {string} 模式</li></ul><p>设置无障碍模式为mode. mode的可选值为：</p><ul><li><code>fast</code> 快速模式. 该模式下会启用控件缓存, 从而选择器获取屏幕控件更快. 对于需要快速的控件查看和操作的脚本可以使用该模式, 一般脚本则没有必要使用该函数.</li><li><code>normal</code> 正常模式, 默认.</li></ul><h2 id="auto-setflags-flags" tabindex="-1">auto.setFlags(flags)</h2><p><strong>[v4.1.0新增]</strong></p><ul><li><code>flags</code> {string} | {Array} 一些标志, 来启用和禁用某些特性, 包括： <ul><li><code>findOnUiThread</code> 使用该特性后, 选择器搜索时会在主进程进行. 该特性用于解决线程安全问题导致的次生问题, 不过目前貌似已知问题并不是线程安全问题.</li><li><code>useUsageStats</code> 使用该特性后, 将会以&quot;使用情况统计&quot;服务的结果来检测当前正在运行的应用包名（需要授予&quot;查看使用情况统计&quot;权限). 如果觉得currentPackage()返回的结果不太准确, 可以尝试该特性.</li><li><code>useShell</code> 使用该特性后, 将使用shell命令获取当前正在运行的应用的包名、活动名称, 但是需要root权限.</li></ul></li></ul><p>启用有关automator的一些特性. 例如：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>auto.setFlags([&quot;findOnUiThread&quot;, &quot;useShell&quot;]);</span></span></code></pre></div><h2 id="auto-service" tabindex="-1">auto.service</h2><p><strong>[v4.1.0新增]</strong></p><ul><li><a href="https://developer.android.com/reference/android/accessibilityservice/AccessibilityService/" target="_blank" rel="noreferrer">AccessibilityService</a></li></ul><p>获取无障碍服务. 如果无障碍服务没有启动, 则返回<code>null</code>.</p><p>参见<a href="https://developer.android.com/reference/android/accessibilityservice/AccessibilityService/" target="_blank" rel="noreferrer">AccessibilityService</a>.</p><h2 id="auto-windows" tabindex="-1">auto.windows</h2><p><strong>[v4.1.0新增]</strong></p><ul><li>{Array}</li></ul><p>当前所有窗口(<a href="https://developer.android.com/reference/android/view/accessibility/AccessibilityWindowInfo/" target="_blank" rel="noreferrer">AccessibilityWindowInfo</a>)的数组, 可能包括状态栏、输入法、当前应用窗口, 弹出窗口、悬浮窗、分屏应用窗口等. 可以分别获取每个窗口的布局信息.</p><p>该函数需要Android 5.0以上才能运行.</p><h2 id="auto-root" tabindex="-1">auto.root</h2><p><strong>[v4.1.0新增]</strong></p><ul><li>{UiObject}</li></ul><p>当前窗口的布局根元素. 如果无障碍服务未启动或者WindowFilter均返回false, 则会返回<code>null</code>.</p><p>如果不设置windowFilter, 则当前窗口即为活跃的窗口（获取到焦点、正在触摸的窗口）；如果设置了windowFilter, 则获取的是过滤的窗口中的第一个窗口.</p><p>如果系统是Android5.0以下, 则始终返回当前活跃的窗口的布局根元素.</p><h2 id="auto-rootinactivewindow" tabindex="-1">auto.rootInActiveWindow</h2><p><strong>[v4.1.0新增]</strong></p><ul><li>{UiObject}</li></ul><p>当前活跃的窗口（获取到焦点、正在触摸的窗口）的布局根元素. 如果无障碍服务未启动则为<code>null</code>.</p><h2 id="auto-setwindowfilter-filter" tabindex="-1">auto.setWindowFilter(filter)</h2><p><strong>[v4.1.0新增]</strong></p><ul><li><code>filter</code> {Function} 参数为窗口(<a href="https://developer.android.com/reference/android/view/accessibility/AccessibilityWindowInfo/" target="_blank" rel="noreferrer">AccessibilityWindowInfo</a>), 返回值为Boolean的函数.</li></ul><p>设置窗口过滤器. 这个过滤器可以决定哪些窗口是目标窗口, 并影响选择器的搜索. 例如, 如果想要选择器在所有窗口（包括状态栏、输入法等）中搜索, 只需要使用以下代码：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>auto.setWindowFilter(function(window){</span></span>
<span class="line"><span>    //不管是如何窗口, 都返回true, 表示在该窗口中搜索</span></span>
<span class="line"><span>    return true;</span></span>
<span class="line"><span>});</span></span></code></pre></div><p>又例如, 当前使用了分屏功能, 屏幕上有Auto.js和QQ两个应用, 但我们只想选择器对QQ界面进行搜索, 则：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>auto.setWindowFilter(function(window){</span></span>
<span class="line"><span>    // 对于应用窗口, 他的title属性就是应用的名称, 因此可以通过title属性来判断一个应用</span></span>
<span class="line"><span>    return window.title == &quot;QQ&quot;;</span></span>
<span class="line"><span>});</span></span></code></pre></div><p>选择器默认是在当前活跃的窗口中搜索, 不会搜索诸如悬浮窗、状态栏之类的, 使用WindowFilter则可以控制搜索的窗口.</p><p>需要注意的是, 如果WindowFilter返回的结果均为false, 则选择器的搜索结果将为空.</p><p>另外setWindowFilter函数也会影响<code>auto.windowRoots</code>的结果.</p><p>该函数需要Android 5.0以上才有效.</p><h2 id="auto-windowroots" tabindex="-1">auto.windowRoots</h2><p><strong>[v4.1.0新增]</strong></p><ul><li>{Array}</li></ul><p>返回当前被WindowFilter过滤的窗口的布局根元素组成的数组.</p><p>如果系统是Android5.0以下, 则始终返回当前活跃的窗口的布局根元素的数组.</p><h1 id="simpleactionautomator" tabindex="-1">SimpleActionAutomator</h1><p>SimpleActionAutomator提供了一些模拟简单操作的函数, 例如点击文字、模拟按键等. 这些函数可以直接作为全局函数使用.</p><h2 id="click-text-i" tabindex="-1">click(text[, i])</h2><ul><li><code>text</code> {string} 要点击的文本</li><li><code>i</code> {number} 如果相同的文本在屏幕中出现多次, 则i表示要点击第几个文本, i从0开始计算</li></ul><p>返回是否点击成功. 当屏幕中并未包含该文本, 或者该文本所在区域不能点击时返回false, 否则返回true.</p><p>该函数可以点击大部分包含文字的按钮. 例如微信主界面下方的&quot;微信&quot;, &quot;联系人&quot;, &quot;发现&quot;, &quot;我&quot;的按钮.<br> 通常与while同时使用以便点击按钮直至成功. 例如:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>while(!click(&quot;扫一扫&quot;));</span></span></code></pre></div><p>当不指定参数i时则会尝试点击屏幕上出现的所有文字text并返回是否全部点击成功.</p><p>i是从0开始计算的, 也就是, <code>click(&quot;啦啦啦&quot;, 0)</code>表示点击屏幕上第一个&quot;啦啦啦&quot;, <code>click(&quot;啦啦啦&quot;, 1)</code>表示点击屏幕上第二个&quot;啦啦啦&quot;.</p><blockquote><p>文本所在区域指的是, 从文本处向其父视图寻找, 直至发现一个可点击的部件为止.</p></blockquote><h2 id="click-left-top-bottom-right" tabindex="-1">click(left, top, bottom, right)</h2><ul><li><code>left</code> {number} 要点击的长方形区域左边与屏幕左边的像素距离</li><li><code>top</code> {number} 要点击的长方形区域上边与屏幕上边的像素距离</li><li><code>bottom</code> {number} 要点击的长方形区域下边与屏幕下边的像素距离</li><li><code>right</code> {number} 要点击的长方形区域右边与屏幕右边的像素距离</li></ul><p>**注意, 该函数一般只用于录制的脚本中使用, 在自己写的代码中使用该函数一般不要使用该函数. **</p><p>点击在指定区域的控件. 当屏幕中并未包含与该区域严格匹配的区域, 或者该区域不能点击时返回false, 否则返回true.</p><p>有些按钮或者部件是图标而不是文字（例如发送朋友圈的照相机图标以及QQ下方的消息、联系人、动态图标）, 这时不能通过<code>click(text, i)</code>来点击, 可以通过描述图标所在的区域来点击. left, bottom, top, right描述的就是点击的区域.</p><p>至于要定位点击的区域, 可以在悬浮窗使用布局分析工具查看控件的bounds属性.</p><p>通过无障碍服务录制脚本会生成该语句.</p><h2 id="longclick-text-i" tabindex="-1">longClick(text[, i]))</h2><ul><li><code>text</code> {string} 要长按的文本</li><li><code>i</code> {number} 如果相同的文本在屏幕中出现多次, 则i表示要长按第几个文本, i从0开始计算</li></ul><p>返回是否点击成功. 当屏幕中并未包含该文本, 或者该文本所在区域不能点击时返回false, 否则返回true.</p><p>当不指定参数i时则会尝试点击屏幕上出现的所有文字text并返回是否全部长按成功.</p><h2 id="scrollup-i" tabindex="-1">scrollUp([i])</h2><ul><li><code>i</code> {number} 要滑动的控件序号</li></ul><p>找到第i+1个可滑动控件上滑或<strong>左滑</strong>. 返回是否操作成功. 屏幕上没有可滑动的控件时返回false.</p><p>另外不加参数时<code>scrollUp()</code>会寻找面积最大的可滑动的控件上滑或左滑, 例如微信消息列表等.</p><p>参数为一个整数i时会找到第i + 1个可滑动控件滑动. 例如<code>scrollUp(0)</code>为滑动第一个可滑动控件.</p><h2 id="scrolldown-i" tabindex="-1">scrollDown([i])</h2><ul><li><code>i</code> {number} 要滑动的控件序号</li></ul><p>找到第i+1个可滑动控件下滑或<strong>右滑</strong>. 返回是否操作成功. 屏幕上没有可滑动的控件时返回false.</p><p>另外不加参数时<code>scrollUp()</code>会寻找面积最大的可滑动的控件下滑或右滑.</p><p>参数为一个整数i时会找到第i + 1个可滑动控件滑动. 例如<code>scrollUp(0)</code>为滑动第一个可滑动控件.</p><h2 id="settext-i-text" tabindex="-1">setText([i, ]text)</h2><ul><li>i {number} 表示要输入的为第i + 1个输入框</li><li>text {string} 要输入的文本</li></ul><p>返回是否输入成功. 当找不到对应的文本框时返回false.</p><p>不加参数i则会把所有输入框的文本都置为text. 例如<code>setText(&quot;测试&quot;)</code>.</p><p>这里的输入文本的意思是, 把输入框的文本置为text, 而不是在原来的文本上追加.</p><h2 id="input-i-text" tabindex="-1">input([i, ]text)</h2><ul><li>i {number} 表示要输入的为第i + 1个输入框</li><li>text {string} 要输入的文本</li></ul><p>返回是否输入成功. 当找不到对应的文本框时返回false.</p><p>不加参数i则会把所有输入框的文本追加内容text. 例如<code>input(&quot;测试&quot;)</code>.</p>`,217)])])}const b=o(d,[["render",c]]);export{h as __pageData,b as default};
