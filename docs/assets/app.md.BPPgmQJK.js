import{_ as t,o as a,c as o,a2 as p}from"./chunks/framework.BZO4qERu.js";const h=JSON.parse('{"title":"通用应用 (App)","description":"","frontmatter":{},"headers":[],"relativePath":"app.md","filePath":"app.md","lastUpdated":1780845803000}'),n={name:"app.md"};function i(s,e,c,l,d,r){return a(),o("div",null,[...e[0]||(e[0]=[p(`<h1 id="通用应用-app" tabindex="-1">通用应用 (App)</h1><h2 id="_6-7-0-源码校对" tabindex="-1">6.7.0 源码校对</h2><p>本页已按 AutoJs6 <code>6.7.0</code> (<code>ed3eb10e88db5a8425fd94bdddefa4176e5e1c94</code>) 对照以下源码路径校对:</p><ul><li><code>app/src/main/java/org/autojs/autojs/runtime/ScriptRuntime.kt</code></li><li><code>app/src/main/java/org/autojs/autojs/runtime/api/augment/app/App.kt</code></li><li><code>app/src/main/java/org/autojs/autojs/runtime/api/AppUtils.kt</code></li></ul><p>运行时中 <code>app</code> / <code>$app</code> 由 augment 注入, 并将 <code>autojs</code> 挂载为 <code>app.autojs</code>. <code>App</code> 模块的主要能力覆盖 Intent 构造与启动、应用启动 / 卸载 / 查询、文件查看编辑、邮件、广播与 URL 打开.</p><p>当前源码公开并全局化的入口包括 <code>intent</code>, <code>startActivity</code>, <code>startDualActivity</code>, <code>intentToShell</code>, <code>startService</code>, <code>sendEmail</code>, <code>sendBroadcast</code>, <code>sendLocalBroadcastSync</code>, <code>parseUri</code>, <code>openUrl</code>, <code>openDualUrl</code>, <code>getUriForFile</code>, <code>getAppByAlias</code>, <code>launch</code>, <code>launchDual</code>, <code>launchPackage</code>, <code>launchDualPackage</code>, <code>launchApp</code>, <code>launchDualApp</code>, <code>getPackageName</code>, <code>getAppName</code>, 设置页相关别名, <code>isInstalled</code>, <code>isDualInstalled</code>, <code>uninstall</code>, <code>uninstallDual</code>, <code>viewFile</code>, <code>editFile</code>, <code>kill</code>, <code>killDual</code>.</p><p><code>startActivity</code> 可接受字符串、Android <code>Intent</code>, URI 或 JavaScript 对象. 字符串包含 <code>://</code> 时按 URL 打开; 形如网址但无协议时会自动补 <code>http://</code>; 其他字符串按 Activity short form 解析. 选项对象可包含 <code>dual</code>, <code>root</code>, <code>shizuku</code> 等字段; root / Shizuku 分支通过 <code>am start</code> shell 命令启动.</p><p>Intent 对象必须是 Android <code>Intent</code> 或可转换的 JavaScript 对象; 非法类型会抛出参数异常.</p><p>app模块提供一系列函数, 用于使用其他应用、与其他应用交互. 例如发送意图、打开文件、发送邮件等.</p><p>同时提供了方便的进阶函数startActivity和sendBroadcast, 用他们可完成app模块没有内置的和其他应用的交互.</p><h2 id="app-versioncode" tabindex="-1">app.versionCode</h2><ul><li>{number}</li></ul><p>当前软件版本号, 整数值. 例如160, 256等.</p><p>如果在Auto.js中运行则为Auto.js的版本号；在打包的软件中则为打包软件的版本号.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>toastLog(app.versionCode);</span></span></code></pre></div><h2 id="app-versionname" tabindex="-1">app.versionName</h2><ul><li>{string}</li></ul><p>当前软件的版本名称, 例如&quot;3.0.0 Beta&quot;.</p><p>如果在Auto.js中运行则为Auto.js的版本名称；在打包的软件中则为打包软件的版本名称.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>toastLog(app.versionName);</span></span></code></pre></div><h2 id="app-autojs-versioncode" tabindex="-1">app.autojs.versionCode</h2><ul><li>{number}</li></ul><p>Auto.js版本号, 整数值. 例如160, 256等.</p><h2 id="app-autojs-versionname" tabindex="-1">app.autojs.versionName</h2><ul><li>{string}</li></ul><p>Auto.js版本名称, 例如&quot;3.0.0 Beta&quot;.</p><h2 id="app-launchapp-appname" tabindex="-1">app.launchApp(appName)</h2><ul><li><code>appName</code> {string} 应用名称</li></ul><p>通过应用名称启动应用. 如果该名称对应的应用不存在, 则返回false; 否则返回true. 如果该名称对应多个应用, 则只启动其中某一个.</p><p>该函数也可以作为全局函数使用.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>launchApp(&quot;Auto.js&quot;);</span></span></code></pre></div><h2 id="app-launch-packagename" tabindex="-1">app.launch(packageName)</h2><ul><li><code>packageName</code> {string} 应用包名</li></ul><p>通过应用包名启动应用. 如果该包名对应的应用不存在, 则返回false；否则返回true.</p><p>该函数也可以作为全局函数使用.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//启动微信</span></span>
<span class="line"><span>launch(&quot;com.tencent.mm&quot;);</span></span></code></pre></div><h2 id="app-launchpackage-packagename" tabindex="-1">app.launchPackage(packageName)</h2><ul><li><code>packageName</code> {string} 应用包名</li></ul><p>相当于<code>app.launch(packageName)</code>.</p><h2 id="app-getpackagename-appname" tabindex="-1">app.getPackageName(appName)</h2><ul><li><code>appName</code> {string} 应用名称</li></ul><p>获取应用名称对应的已安装的应用的包名. 如果该找不到该应用, 返回null；如果该名称对应多个应用, 则只返回其中某一个的包名.</p><p>该函数也可以作为全局函数使用.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>var name = getPackageName(&quot;QQ&quot;); //返回&quot;com.tencent.mobileqq&quot;</span></span></code></pre></div><h2 id="app-getappname-packagename" tabindex="-1">app.getAppName(packageName)</h2><ul><li><code>packageName</code> {string} 应用包名</li></ul><p>获取应用包名对应的已安装的应用的名称. 如果该找不到该应用, 返回null.</p><p>该函数也可以作为全局函数使用.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>var name = getAppName(&quot;com.tencent.mobileqq&quot;); //返回&quot;QQ&quot;</span></span></code></pre></div><h2 id="app-openappsetting-packagename" tabindex="-1">app.openAppSetting(packageName)</h2><ul><li><code>packageName</code> {string} 应用包名</li></ul><p>打开应用的详情页(设置页). 如果找不到该应用, 返回false; 否则返回true.</p><p>该函数也可以作为全局函数使用.</p><h2 id="app-viewfile-path" tabindex="-1">app.viewFile(path)</h2><ul><li><code>path</code> {string} 文件路径</li></ul><p>用其他应用查看文件. 文件不存在的情况由查看文件的应用处理.</p><p>如果找不出可以查看该文件的应用, 则抛出<code>ActivityNotException</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//查看文本文件</span></span>
<span class="line"><span>app.viewFile(&quot;/sdcard/1.txt&quot;);</span></span></code></pre></div><h2 id="app-editfile-path" tabindex="-1">app.editFile(path)</h2><ul><li><code>path</code> {string} 文件路径</li></ul><p>用其他应用编辑文件. 文件不存在的情况由编辑文件的应用处理.</p><p>如果找不出可以编辑该文件的应用, 则抛出<code>ActivityNotException</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//编辑文本文件</span></span>
<span class="line"><span>app.editFile(&quot;/sdcard/1.txt/);</span></span></code></pre></div><h2 id="app-uninstall-packagename" tabindex="-1">app.uninstall(packageName)</h2><ul><li><code>packageName</code> {string} 应用包名</li></ul><p>卸载应用. 执行后会会弹出卸载应用的提示框. 如果该包名的应用未安装, 由应用卸载程序处理, 可能弹出&quot;未找到应用&quot;的提示.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//卸载QQ</span></span>
<span class="line"><span>app.uninstall(&quot;com.tencent.mobileqq&quot;);</span></span></code></pre></div><h2 id="app-openurl-url" tabindex="-1">app.openUrl(url)</h2><ul><li><code>url</code> {string} 网站的Url, 如果不以&quot;http://&quot;或&quot;https://&quot;开头则默认是&quot;http://&quot;.</li></ul><p>用浏览器打开网站url.</p><p>如果没有安装浏览器应用, 则抛出<code>ActivityNotException</code>.</p><h2 id="app-sendemail-options" tabindex="-1">app.sendEmail(options)</h2><ul><li><code>options</code> {Object} 发送邮件的参数. 包括:</li><li><code>email</code> {string} | {Array} 收件人的邮件地址. 如果有多个收件人, 则用字符串数组表示</li><li><code>cc</code> {string} | {Array} 抄送收件人的邮件地址. 如果有多个抄送收件人, 则用字符串数组表示</li><li><code>bcc</code> {string} | {Array} 密送收件人的邮件地址. 如果有多个密送收件人, 则用字符串数组表示</li><li><code>subject</code> {string} 邮件主题(标题)</li><li><code>text</code> {string} 邮件正文</li><li><code>attachment</code> {string} 附件的路径.</li></ul><p>根据选项options调用邮箱应用发送邮件. 这些选项均是可选的.</p><p>如果没有安装邮箱应用, 则抛出<code>ActivityNotException</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//发送邮件给10086@qq.com和10001@qq.com.</span></span>
<span class="line"><span>app.sendEmail({</span></span>
<span class="line"><span>    email: [&quot;10086@qq.com&quot;, &quot;10001@qq.com&quot;],</span></span>
<span class="line"><span>    subject: &quot;这是一个邮件标题&quot;,</span></span>
<span class="line"><span>    text: &quot;这是邮件正文&quot;</span></span>
<span class="line"><span>});</span></span></code></pre></div><h2 id="app-startactivity-name" tabindex="-1">app.startActivity(name)</h2><ul><li><code>name</code> {string} 活动名称, 可选的值为: <ul><li><code>console</code> 日志界面</li><li><code>settings</code> 设置界面</li></ul></li></ul><p>启动Auto.js的特定界面. 该函数在Auto.js内运行则会打开Auto.js内的界面, 在打包应用中运行则会打开打包应用的相应界面.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app.startActivity(&quot;console&quot;);</span></span></code></pre></div><h2 id="app-intent-options" tabindex="-1">app.intent(options)</h2><ul><li><code>options</code> {Object} 选项, 包括： <ul><li><p><code>action</code> {string} 意图的Action, 指意图要完成的动作, 是一个字符串常量, 比如&quot;android.intent.action.SEND&quot;. 当action以&quot;android.intent.action&quot;开头时, 可以省略前缀, 直接用&quot;SEND&quot;代替. 参见<a href="https://developer.android.com/reference/android/content/Intent.html#standard-activity-actions/" target="_blank" rel="noreferrer">Actions</a>.</p></li><li><p><code>type</code> {string} 意图的MimeType, 表示和该意图直接相关的数据的类型, 表示比如&quot;text/plain&quot;为纯文本类型.</p></li><li><p><code>data</code> {string} 意图的Data, 表示和该意图直接相关的数据, 是一个Uri, 可以是文件路径或者Url等. 例如要打开一个文件, action为&quot;android.intent.action.VIEW&quot;, data为&quot;file:///sdcard/1.txt&quot;.</p></li><li><p><code>category</code> {Array} 意图的类别. 比较少用. 参见<a href="https://developer.android.com/reference/android/content/Intent.html#standard-categories/" target="_blank" rel="noreferrer">Categories</a>.</p></li><li><p><code>packageName</code> {string} 目标包名</p></li><li><p><code>className</code> {string} 目标Activity或Service等组件的名称</p></li><li><p><code>extras</code> {Object} 以键值对构成的这个Intent的Extras(额外信息). 提供该意图的其他信息, 例如发送邮件时的邮件标题、邮件正文. 参见<a href="https://developer.android.com/reference/android/content/Intent.html#standard-extra-data/" target="_blank" rel="noreferrer">Extras</a>.</p></li><li><p><code>flags</code> {Array} intent的标识, 字符串数组, 例如<code>[&quot;activity_new_task&quot;, &quot;grant_read_uri_permission&quot;]</code>. 参见<a href="https://developer.android.com/reference/android/content/Intent.html#setFlags%28int%29/" target="_blank" rel="noreferrer">Flags</a>.</p><p><strong>[v4.1.0新增]</strong></p></li><li><p><code>root</code> {Boolea} 是否以root权限启动、发送该intent. 使用该参数后, 不能使用<code>context.startActivity()</code>等方法, 而应该直接使用诸如<code>app.startActivity({...})</code>的方法.</p><p><strong>[v4.1.0新增]</strong></p></li></ul></li></ul><p>根据选项, 构造一个意图Intent对象.</p><p>例如：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//打开应用来查看图片文件</span></span>
<span class="line"><span>var i = app.intent({</span></span>
<span class="line"><span>    action: &quot;VIEW&quot;,</span></span>
<span class="line"><span>    type: &quot;image/png&quot;,</span></span>
<span class="line"><span>    data: &quot;file:///sdcard/1.png&quot;</span></span>
<span class="line"><span>});</span></span>
<span class="line"><span>context.startActivity(i);</span></span></code></pre></div><p>需要注意的是, 除非应用专门暴露Activity出来, 否则在没有root权限的情况下使用intent是无法跳转到特定Activity、应用的特定界面的. 例如我们能通过Intent跳转到QQ的分享界面, 是因为QQ对外暴露了分享的Activity；而在没有root权限的情况下, 我们无法通过intent跳转到QQ的设置界面, 因为QQ并没有暴露这个Activity.</p><p>但如果有root权限, 则在intent的参数加上<code>&quot;root&quot;: true</code>即可. 例如使用root权限跳转到Auto.js的设置界面为：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app.startActivity({</span></span>
<span class="line"><span>    packageName: &quot;org.autojs.autojs&quot;,</span></span>
<span class="line"><span>    className: &quot;org.autojs.autojs.ui.settings.SettingsActivity_&quot;,</span></span>
<span class="line"><span>    root: true</span></span>
<span class="line"><span>});</span></span></code></pre></div><p>另外, 关于intent的参数如何获取的问题, 一些intent是意外发现并且在网络中传播的（例如跳转QQ聊天窗口是因为QQ给网页提供了跳转到客服QQ的方法）, 如果要自己获取活动的intent的参数, 可以通过例如&quot;intent记录&quot;, &quot;隐式启动&quot;等应用拦截内部intent或者查询暴露的intent. 其中拦截内部intent需要XPosed框架, 或者可以通过反编译等手段获取参数. 总之, 没有简单直接的方法.</p><p>更多信息, 请百度<a href="https://www.baidu.com/s?wd=android%20Intent" target="_blank" rel="noreferrer">安卓Intent</a>或参考<a href="https://developer.android.com/guide/components/intents-filters.html#Types" target="_blank" rel="noreferrer">Android指南: Intent</a>.</p><h2 id="app-startactivity-options" tabindex="-1">app.startActivity(options)</h2><ul><li><code>options</code> {Object} 选项</li></ul><p>根据选项构造一个Intent, 并启动该Activity.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app.startActivity({</span></span>
<span class="line"><span>    action: &quot;SEND&quot;,</span></span>
<span class="line"><span>    type: &quot;text/plain&quot;,</span></span>
<span class="line"><span>    data: &quot;file:///sdcard/1.txt&quot;</span></span>
<span class="line"><span>});</span></span></code></pre></div><h2 id="app-sendbroadcast-options" tabindex="-1">app.sendBroadcast(options)</h2><ul><li><code>options</code> {Object} 选项</li></ul><p>根据选项构造一个Intent, 并发送该广播.</p><h2 id="app-startservice-options" tabindex="-1">app.startService(options)</h2><ul><li><code>options</code> {Object} 选项</li></ul><p>根据选项构造一个Intent, 并启动该服务.</p><h2 id="app-sendbroadcast-name" tabindex="-1">app.sendBroadcast(name)</h2><p><strong>[v4.1.0新增]</strong></p><ul><li><code>name</code> {string} 特定的广播名称, 包括： <ul><li><code>inspect_layout_hierarchy</code> 布局层次分析</li><li><code>inspect_layout_bounds</code> 布局范围</li></ul></li></ul><p>发送以上特定名称的广播可以触发Auto.js的布局分析, 方便脚本调试. 这些广播在Auto.js发送才有效, 在打包的脚本上运行将没有任何效果.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>app.sendBroadcast(&quot;inspect_layout_bounds&quot;);</span></span></code></pre></div><h2 id="app-intenttoshell-options" tabindex="-1">app.intentToShell(options)</h2><p><strong>[v4.1.0新增]</strong></p><ul><li><code>options</code> {Object} 选项</li></ul><p>根据选项构造一个Intent, 转换为对应的shell的intent命令的参数.</p><p>例如:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>shell(&quot;am start &quot; + app.intentToShell({</span></span>
<span class="line"><span>    packageName: &quot;org.autojs.autojs&quot;,</span></span>
<span class="line"><span>    className: &quot;org.autojs.autojs.ui.settings.SettingsActivity_&quot;</span></span>
<span class="line"><span>}), true);</span></span></code></pre></div><p>参见<a href="https://developer.android.com/studio/command-line/adb#IntentSpec/" target="_blank" rel="noreferrer">intent参数的规范</a>.</p><h2 id="app-parseuri-uri" tabindex="-1">app.parseUri(uri)</h2><p><strong>[v4.1.0新增]</strong></p><ul><li><code>uri</code> {string} 一个代表Uri的字符串, 例如&quot;file:///sdcard/1.txt&quot;, &quot;<a href="https://www.autojs.org" target="_blank" rel="noreferrer">https://www.autojs.org</a>&quot;</li><li>返回 {Uri} 一个代表Uri的对象, 参见<a href="https://developer.android.com/reference/android/net/Uri/" target="_blank" rel="noreferrer">android.net.Uri</a>.</li></ul><p>解析uri字符串并返回相应的Uri对象. 即使Uri格式错误, 该函数也会返回一个Uri对象, 但之后如果访问该对象的scheme, path等值可能因解析失败而返回<code>null</code>.</p><p>需要注意的是, 在高版本Android上, 由于系统限制直接在Uri暴露文件的绝对路径, 因此如果uri字符串是文件<code>file://...</code>, 返回的Uri会是诸如<code>content://...</code>的形式.</p><h2 id="app-geturiforfile-path" tabindex="-1">app.getUriForFile(path)</h2><p><strong>[v4.1.0新增]</strong></p><ul><li><code>path</code> {string} 文件路径, 例如&quot;/sdcard/1.txt&quot;</li><li>返回 {Uri} 一个指向该文件的Uri的对象, 参见<a href="https://developer.android.com/reference/android/net/Uri/" target="_blank" rel="noreferrer">android.net.Uri</a>.</li></ul><p>从一个文件路径创建一个uri对象. 需要注意的是, 在高版本Android上, 由于系统限制直接在Uri暴露文件的绝对路径, 因此返回的Uri会是诸如<code>content://...</code>的形式.</p><h2 id="app-getinstalledapps-options" tabindex="-1">app.getInstalledApps([options])</h2><p>** [<a href="https://pro.autojs.org//" target="_blank" rel="noreferrer">Pro 8.0.0新增</a>] **</p><ul><li><code>options</code> {Object} 选项, 包括： <ul><li><code>get</code>: 指定返回的应用信息中包含的信息 <ul><li><code>&quot;activities&quot;</code> 应用的Activity组件信息</li><li><code>&quot;configurations&quot;</code> 应用的硬件配置</li><li><code>&quot;gids&quot;</code> 应用的group id</li><li><code>&quot;instrumentation&quot;</code> 应用的Instrumentation信息</li><li><code>&quot;intent_filters&quot;</code> 应用的意图过滤</li><li><code>&quot;meta_data&quot;</code> 应用的元信息（默认）</li><li><code>&quot;permissions&quot;</code> 应用的权限信息</li><li><code>&quot;providers&quot;</code> 应用的ContentProvider组件信息</li><li><code>&quot;receivers&quot;</code> 应用的BroadcastReceiver组件信息</li><li><code>&quot;services&quot;</code> 应用的Service组件信息</li><li><code>&quot;shared_library_files&quot;</code> 应用的动态链接库文件信息</li><li><code>&quot;signatures&quot;</code> 应用的签名信息（已弃用</li><li><code>&quot;signing_certificates&quot;</code> 应用的签名信息</li><li><code>&quot;uri_permission_patterns&quot;</code></li><li><code>&quot;disabled_components&quot;</code> 被卸载的但保留了数据的应用</li><li><code>&quot;disabled_until_used_components&quot;</code> 禁用直到被使用的组件</li><li><code>&quot;uninstalled_packages&quot;</code> 被卸载的但保留了数据的应用</li></ul></li><li><code>match</code>: 指定要匹配的应用列表 <ul><li><code>&quot;uninstalled_packages&quot;</code> 被卸载的但保留了数据的应用</li><li><code>&quot;disabled_components&quot;</code> 被禁用的组件</li><li><code>&quot;disabled_until_used_components&quot;</code> 禁用直到被使用的组件</li><li><code>&quot;system_only&quot;</code> 只匹配系统应用</li><li><code>&quot;factory_only&quot;</code> 只匹配预装应用</li><li><code>&quot;apex&quot;</code> APEX应用</li></ul></li></ul></li><li>返回 {Array&lt;ApplicationInfo&gt;}</li></ul><p>返回为当前用户安装的所有应用程序包的列表. 如果设置了match选项 <code>uninstalled_packages</code>, 则包括被删除但保留了数据的应用程序. 获取安装的应用列表.</p><p>返回值是ApplicationInfo对象的数组. 如果没有安装任何应用, 则返回一个空数组.</p><p>选项options的match选项用于指定要返回哪些应用程序, get选项用于指定返回的应用程序携带哪些信息.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let apps = $app.getInstalledApps({</span></span>
<span class="line"><span>    matcg</span></span>
<span class="line"><span>})</span></span></code></pre></div>`,128)])])}const g=t(n,[["render",i]]);export{h as __pageData,g as default};
